import * as React from "react"
import PropTypes from "prop-types"

import "./layout.css"
import colors from "tailwindcss/colors"

const BillCard = ({ children }) => {

  return (
    <>
    {/*  To Create */}
    </>
  )
}

BillCard.propTypes = {
  children: PropTypes.node.isRequired
}

export default BillCard
